
# name
name: hello world

# trigger
on:
- push

# one or more jobs
jobs:
    greetings:
        runs-on: ubuntu-latest
        steps:
            - id: s00
              # running some shell bash command
              run: echo "Current time in github is $(date)"
            - id: s01
              run: cat README.md
            - id: s02
              run: ls -l